%{
#pragma once

#ifdef __GNUC__
#pragma GCC diagnostic push
#pragma GCC diagnostic ignored "-Wunused-parameter"
#endif

%}

struct test {
    char *name;

    enum {
        TESTS_ALL,
        TESTS_TEST,
        TESTS_LOG,
    } value;
};

%%
all, TESTS_ALL
test, TESTS_TEST
log, TESTS_LOG
%%
#ifdef __GNUC__
#pragma GCC diagnostic pop
#endif
